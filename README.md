1. on Windows, run python -m venv venv. On Unix, run pyvenv venv
2. On Windows, execute venv\Scripts\activate, on Linux run "source venv/bin/activate"
3. Run "pip install -r requirements.txt"
4. Run "python run.py"
